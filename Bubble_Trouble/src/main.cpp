#include "Menu.h"

int main()
{
	Menu().run();
	//Socket socket;
	//int input;
	//std::cin >> input;
	//socket.setMode(input);
	//socket.openHost();
	//socket.connect("127.0.0.1");
	//socket.send();
	//socket.receive();
	return EXIT_SUCCESS;
}